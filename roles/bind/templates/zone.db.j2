$TTL	604800
@       IN      SOA     ns1.{{ startup_name }}. admin.{{ startup_name }}. (
                              5         ; Serial
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                         604800 )       ; Negative Cache TTL

     IN      NS      ns1.{{ startup_name }}.

ns1.{{ startup_name }}.    IN    A    {{ dns_host }}

{% for host in groups['remote_hosts'] %}
{{ host }}.{{ startup_name }}.    IN    A    {{ hostvars[host]['ansible_default_ipv4']['address'] }}
{{ host }}.    IN    A    {{ hostvars[host]['ansible_default_ipv4']['address'] }}
{% endfor %}